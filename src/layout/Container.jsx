import React from "react";
import { ContainerChildren } from "../components/";

const Container = ({ children }) => {
  return <ContainerChildren>{children}</ContainerChildren>;
};

export default Container;
